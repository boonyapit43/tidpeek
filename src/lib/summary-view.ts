export type SummaryView = "day" | "week" | "month" | "year";

/**
 * มุมมองที่หน้าสรุปเปิดมาเจอ เมื่อไม่ได้ระบุใน URL
 *
 * เป็นค่าตายตัว ไม่ใช่การเดาจากข้อมูล
 *
 * เคยเป็นตรรกะที่เลือกช่วงแคบสุดที่ยังมีข้อมูล ซึ่งกันหน้าว่างได้ แต่แลกมา
 * ด้วยการที่เปิดแอปสองวันติดกันแล้วเจอคนละแท็บ ทั้งที่ไม่ได้ทำอะไรต่างกัน
 * — ของที่เดาให้ฉลาดแต่คาดเดาไม่ได้ ใช้ยากกว่าของที่คงที่
 *
 * ⚠️ เคยเป็น "สัปดาห์" ด้วยเหตุผลว่าแทบไม่มีวันว่าง เจ้าของร้านเปลี่ยนเป็น
 *    "วัน" เอง ซึ่งตรงกับจังหวะการใช้จริงมากกว่า — เขาบันทึกตอนปิดร้าน
 *    (98% ของรายการถูกบันทึกระหว่างสามทุ่มถึงตีหนึ่ง) แล้วดูสรุปของวันนั้น
 *    ทันทีหลังลงเสร็จ ซึ่งเป็นจังหวะที่ "วันนี้" มีข้อมูลครบพอดี
 *
 *    ข้อแลกเปลี่ยนที่ยังอยู่: เปิดแอปตอนกลางวันก่อนลงรายการจะเจอหน้าว่าง
 *    ซึ่งรับได้เพราะแท็บสัปดาห์อยู่ห่างไปแค่แตะเดียว
 */
export const DEFAULT_SUMMARY_VIEW: SummaryView = "day";
