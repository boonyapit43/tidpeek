/**
 * ชื่อของหน้าสำหรับโปรแกรมอ่านหน้าจอ
 *
 * ดีไซน์ไม่ได้พิมพ์ชื่อหน้าไว้บนจอ เพราะแถบเมนูล่างเน้นช่องที่อยู่ให้เห็น
 * อยู่แล้ว และพื้นที่บนมือถือมีจำกัด แต่คนที่ใช้โปรแกรมอ่านหน้าจอไม่เห็น
 * ว่าช่องไหนถูกเน้น จึงไม่มีอะไรบอกเลยว่าตอนนี้เปิดหน้าอะไรอยู่
 *
 * ผลข้างเคียงอีกอย่างของการไม่มี h1 คือหัวข้อในหน้าเริ่มที่ h2 ทำให้โครง
 * หัวข้อของทุกหน้าขาดระดับบนสุด ซึ่งเป็นระดับที่คำสั่ง "ข้ามไปหัวข้อถัดไป"
 * ใช้เป็นจุดตั้งต้น
 *
 * sr-only ซ่อนจากสายตาแต่ยังอยู่ในหน้าจริง ต่างจาก display:none ที่หายไป
 * จากทั้งสองทาง
 */
export function PageTitle({ children }: { children: React.ReactNode }) {
  return <h1 className="sr-only">{children}</h1>;
}
