# บัญชีร้าน

บันทึกรายรับรายจ่ายรายวัน สรุปกำไรและเปอร์เซ็นต์แบบ **รายวัน รายเดือน รายปี แยกตามร้าน** และดูยอดคงเหลือแยกรายบัญชี ออกแบบให้ใช้บนมือถือเป็นหลัก

```bash
npm install
cp .env.example .env.local     # เติม DATABASE_URL, APP_PIN, AUTH_SECRET
npm run db:migrate && npm run db:seed
npm run dev
```

---

## Stack

| ชั้น | ใช้อะไร | ทำไม |
|---|---|---|
| Framework | Next.js 16 App Router | Server Component อ่าน · Server Action เขียน · ไม่ต้องมี API แยก |
| ฐานข้อมูล | Postgres ผ่าน `postgres.js` | ต่อด้วย `DATABASE_URL` อย่างเดียว ย้ายไป Supabase / Neon / VPS ได้โดยไม่แตะโค้ด |
| ORM | Drizzle + Drizzle Kit | type มาจาก schema จริง และมี migration history |
| ตรวจข้อมูล | Zod | ทุก input จาก browser ผ่านด่านนี้ก่อนถึงฐานข้อมูล |
| หน้าตา | Tailwind v4 | mobile-first |
| ล็อกอิน | PIN + JWT ใน httpOnly cookie (`jose`) | คนใช้ไม่กี่คนและไว้ใจกัน · session อายุ 1 วัน |

## เส้นทางการใช้งาน

```
/login      ใส่ PIN (มีแป้นตัวเลขบนจอ)
   ↓
/shops      เลือกร้าน — กล่องเรียงกัน แตะเพื่อเข้า · เพิ่ม เปลี่ยนชื่อ ลบ ได้จากหน้านี้
   ↓
/           บันทึกรายการ
/day        รายวัน — แตะรายการเพื่อแก้หรือลบ
/summary    สรุป 3 โหมด: รายวัน · รายเดือน · รายปี
/settings   บัญชี · ประเภท · ส่งออกข้อมูล
```

หน้าสรุปทั้งสามโหมดใช้ `getSummary()` ตัวเดียวกัน ต่างกันแค่ขอบเขตของช่วงวัน ยอดของวันจึงบวกขึ้นเป็นเดือน และเดือนบวกขึ้นเป็นปีได้ตรงกันเสมอ ไม่มีทางที่แต่ละโหมดจะคิดกันคนละวิธี

การลบร้านลบรายการทั้งหมดของร้านนั้นตามไปด้วยใน transaction เดียว — จำเป็น ไม่งั้นยอดของบัญชีกลางที่ร้านอื่นใช้อยู่จะยังนับเงินของร้านที่ลบไปแล้ว ส่วนบัญชีและประเภท**กลาง**ไม่ถูกแตะ

**ทำไมต้องเลือกร้านก่อนเสมอ** — "กำลังบันทึกลงร้านไหน" เป็นสิ่งที่ผิดไม่ได้ในแอปบัญชี ระบบจึงไม่เดาร้านให้ ไม่ว่าจะเพิ่งล็อกอินหรือร้านที่เคยเลือกถูกลบไป จะเด้งกลับมาหน้านี้ให้เลือกเองทุกครั้ง

---

## แนวคิดที่ต้องเข้าใจก่อนแก้โค้ด

### 1. เงินเป็น `string` ตลอดทาง

Postgres เก็บเป็น `numeric(12,2)` ซึ่งแม่นระดับสตางค์ แต่ `number` ของ JavaScript แทนทศนิยมฐานสิบไม่ได้ครบ พอบวกกันหลายบรรทัดจะเพี้ยนสะสม

**การรวมยอดทุกครั้งเกิดใน SQL** — [src/db/queries.ts](src/db/queries.ts) ฝั่ง TypeScript แปลงเป็นตัวเลขได้ครั้งเดียวตอนแสดงผลเท่านั้น ([src/lib/money.ts](src/lib/money.ts))

ยอดรวมของเดือนจึงเป็น query แยก ไม่ใช่การเอายอดรายวันมาบวกกันใน JS

### 2. ธง `counts`

ประเภทอย่าง *เติมทุน / เงินกู้ / ถอนใช้ส่วนตัว / โอนย้ายบัญชี* มีเงินเดินจริงแต่ไม่ใช่กำไรหรือขาดทุน จึงตั้ง `counts = false`

> **กฎเหล็ก** ธงนี้ใช้กรองตอน**คิดกำไร**เท่านั้น **ห้ามกรองตอนคิดยอดบัญชี**
> ไม่งั้นยอดในแอปจะไม่ตรงกับยอดในแอปธนาคาร

ทั้งสองกรณีถูกบังคับไว้ที่ [src/db/queries.ts](src/db/queries.ts) แล้ว

### 3. ยอดคงเหลือคำนวณสด ไม่ได้เก็บไว้

`opening_balance + เงินเข้า − เงินออก` คิดใหม่ทุกครั้งที่เรียก ยอดที่เก็บไว้เป็นคอลัมน์จะเพี้ยนทันทีที่อัปเดตพลาดครั้งเดียว แล้วไม่มีทางรู้ว่าเพี้ยนตั้งแต่เมื่อไหร่

`opening_balance` เป็นของ **บัญชี** ไม่ใช่ของร้าน — บัญชีที่ใช้ร่วมสองร้านมียอดตั้งต้นใบเดียว ส่วน `shopDelta` ตอบว่า *"ร้านนี้ทำให้เงินในบัญชีเปลี่ยนไปเท่าไหร่"* ไม่ใช่ *"ร้านนี้เหลือเท่าไหร่"*

### 4. วันที่ต้องไม่เลื่อน

- `txn_date` เป็นชนิด `date` ของ Postgres — ไม่มีเวลา ไม่มี timezone
- Drizzle ตั้ง `mode: "string"` ไว้ ค่าจึงเป็น `"YYYY-MM-DD"` ตลอด ไม่กลายเป็น `Date` ที่ถูกตีความเป็น UTC
- **"วันนี้" คำนวณจาก `Asia/Bangkok` เสมอ** ([src/lib/date.ts](src/lib/date.ts)) ไม่พึ่งเวลาเครื่องเซิร์ฟเวอร์ — Vercel รันเป็น UTC ถ้าปล่อยตามเครื่อง รายการที่บันทึกห้าทุ่มครึ่งจะไปลงวันถัดไปทุกคืน
- การจัดกลุ่มวัน/เดือน/ปีใช้ `to_char` บนคอลัมน์ `date` ล้วน ไม่มีการแปลง timezone

### 5. ลบแบบไม่ลบจริง

ทุกตารางมี `is_deleted`, `created_at`, `updated_at` การลบในแอปคือการตั้งธง `is_deleted = true` ไม่ใช่ `DELETE` แถวออก เพราะในระบบบัญชีการลบแถวจริงคือการทำลายหลักฐาน และรายการที่อ้างถึงแถวนั้นจะกลายเป็นเด็กกำพร้าทันที

> **ทุก query ต้องกรอง `is_deleted = false` เสมอ รวมถึง subquery ที่ซ้อนอยู่ข้างใน**
> ลืมที่เดียวแล้วรายการที่ลบไปแล้วจะยังเดินยอดบัญชีอยู่ ซึ่งเป็นบั๊กที่หายากมาก เพราะตัวเลขผิดแค่บางบัญชีและไม่มี error ให้เห็น

`is_active` เป็นคนละเรื่อง — คือปิดใช้งานชั่วคราว ยังเห็นในหน้าตั้งค่าและเปิดกลับได้

วิธีกู้ของที่ลบไปแล้วอยู่ท้ายไฟล์ [supabase/schema.sql](supabase/schema.sql)

### 6. ทุก query ผูกร้านเสมอ

ทุกฟังก์ชันที่แตะข้อมูลร้านรับ `shopId` เป็นพารามิเตอร์แรกแบบบังคับ ลืมส่งแล้ว TypeScript ฟ้องตั้งแต่ตอนเขียน ไม่ใช่ไปเจอตอนข้อมูลร้านหนึ่งโผล่ในอีกร้าน

---

## ความปลอดภัย

Server action เป็น HTTP endpoint ที่ยิงตรงจากอินเทอร์เน็ตได้ ไม่ได้ปลอดภัยเพราะปุ่มที่เรียกมันอยู่หลังหน้าล็อกอิน ทุก action จึงตรวจสามชั้น

1. **ล็อกอินอยู่จริงไหม** — `hasSession()` ทุกตัว ไม่มีข้อยกเว้น
2. **ข้อมูลผ่าน Zod ไหม** — [src/lib/validation.ts](src/lib/validation.ts)
3. **บัญชี/ประเภทที่อ้างถึงเป็นของร้านนี้จริงไหม** — id ใน dropdown แก้ได้จาก devtools ถ้าไม่ตรวจจะผูกรายการข้ามร้านได้

เพิ่มเติม — ทุก `UPDATE`/`DELETE` มี `shop_id` ในเงื่อนไข `WHERE`, PIN เทียบแบบ constant-time, จำกัดจำนวนครั้งที่กรอกผิด, และ [drizzle/0001_enable_rls.sql](drizzle/0001_enable_rls.sql) ปิด REST API อัตโนมัติของ Supabase ทิ้ง

---

## เรื่องหน้าจอ

สามขนาด แต่ละขนาดเปลี่ยนโครง ไม่ใช่แค่ย่อขยาย

| ขนาด | โครง |
|---|---|
| มือถือ | เมนูตรึงขอบล่าง (นิ้วโป้งเอื้อมถึง) · ยอดบัญชีเลื่อนแนวนอนแบบ snap · เนื้อหาคอลัมน์เดียว |
| iPad ขึ้นไป (≥768) | เมนูขึ้นไปอยู่บน · ยอดบัญชีเป็นแถบข้างที่ตรึงไว้ เห็นยอดตลอดเวลาที่กรอก · เนื้อหาสองคอลัมน์ |
| จอกว้าง (≥1024) | เหมือน iPad แต่แถบข้างและเนื้อหากว้างขึ้น |

แยกที่ 768px ไม่ใช่ 1024px เพราะ iPad แนวตั้งมีที่เหลือมากพอแล้ว ถ้าปล่อยเป็นคอลัมน์เดียวจะเหมือนมือถือที่ถูกยืดออก



| ทำอะไร | เพราะอะไร |
|---|---|
| ทุกสิ่งที่กดได้สูง ≥ 44px | ต่ำกว่านี้นิ้วโป้งกดพลาดบ่อย |
| ช่องกรอกตัวอักษร ≥ 16px | iOS ซูมหน้าจอเองถ้าเล็กกว่านี้ แล้วซูมกลับให้ไม่เป็น |
| เมนูอยู่ขอบล่าง (จอเล็ก) | นิ้วโป้งเอื้อมถึงง่ายกว่าขอบบนตอนถือมือเดียว |
| `env(safe-area-inset-*)` | ไม่ให้แถบ home ของ iPhone บังปุ่ม |
| ช่องเงินใช้ `inputMode="decimal"` | เปิดแป้นตัวเลขที่มีจุดทศนิยม |
| ปุ่มลัด "วันนี้ / เมื่อวาน" | เร็วกว่าเปิดปฏิทินแล้วเลื่อนหา |
| ล็อกปุ่มระหว่างส่ง | เน็ตช้าแล้วกดซ้ำจะได้รายการซ้ำสองบรรทัด |
| ปุ่มลบอยู่ในหน้าแก้ไข ไม่ใช่ข้างแถว | ปุ่มลบเล็กๆ ข้างแถวกดโดนโดยไม่ตั้งใจง่ายมากตอนเลื่อน |
| การ์ดแทนตารางบนจอเล็ก | สี่คอลัมน์บนจอ 375px จะบีบจนเลขขึ้นบรรทัดใหม่ |
| `tabular-nums` ทุกตัวเลข | เลขในคอลัมน์เดียวกันต้องตรงแนวถึงจะเทียบกันได้ |
| ไม่พึ่งสีอย่างเดียว | ทุกที่ที่ใช้เขียว/แดง มีเครื่องหมาย `+` `−` หรือข้อความกำกับ |
| แป้นตัวเลขบนจอที่หน้าล็อกอิน | ปุ่มใหญ่กว่าแป้นของระบบ และหน้าจอไม่ต้องขยับหนีคีย์บอร์ด |
| หน้าล็อกอินพอดีหนึ่งจอ | ใช้ `h-dvh` แล้วให้แป้นยืดหดเอง ไม่ต้องเลื่อนทั้งบน SE และ Pro Max |

**โหมดสว่างและมืด** ตามการตั้งค่าของเครื่อง

> ⚠️ ห้ามเขียน `@theme` ซ้อนใน `@media (prefers-color-scheme: dark)` — Tailwind v4 จะเอาบล็อกหลังไปทับบล็อกแรกทั้งหมด ผลคือได้สีโหมดมืดตลอดเวลาและโหมดสว่างหายไปเงียบๆ วิธีที่ถูกคือสลับค่าที่ CSS variable ดิบใน `:root` แล้วให้ `@theme inline` ชี้มาที่ตัวแปรนั้น ดู [globals.css](src/app/globals.css)

---

## คำสั่ง

```bash
npm run dev           # เซิร์ฟเวอร์สำหรับพัฒนา
npm run check         # typecheck + lint + test (รันก่อน commit)
npm run test          # เทสอย่างเดียว
npm run test:watch    # เทสแบบรันซ้ำเมื่อแก้ไฟล์
npm run build         # build สำหรับ production
npm run pack          # ประกอบโฟลเดอร์ deploy/ สำหรับอัปโหลดขึ้นโฮสต์

npm run db:check      # ตรวจว่าต่อฐานติด schema ครบ RLS เปิด (อ่านอย่างเดียว)
npm run db:generate   # สร้าง migration จาก schema ที่แก้
npm run db:migrate    # รัน migration ที่ยังไม่ได้รัน
npm run db:studio     # เปิดหน้าเว็บดูข้อมูลในฐาน
npm run db:seed       # ใส่ข้อมูลตั้งต้น (รันซ้ำได้ ไม่ทำอะไรถ้ามีข้อมูลแล้ว)
```

---

## เทส

`npm run test` — 59 เทส ครอบคลุมตรรกะที่พังแล้วเสียหายเงียบๆ

| ไฟล์ | ครอบอะไร |
|---|---|
| [date.test.ts](src/lib/date.test.ts) | "วันนี้" ต้องมาจาก `Asia/Bangkok` เสมอ · ขอบเดือน/ปี/อธิกสุรทิน · พ.ศ. |
| [money.test.ts](src/lib/money.test.ts) | การจัดรูปแบบเงิน · ซ่อน `.00` แต่ห้ามปัดสตางค์ทิ้ง · หารด้วยศูนย์ |
| [validation.test.ts](src/lib/validation.test.ts) | จำนวนเงินติดลบ/ทศนิยมเกิน · วันที่ไม่มีจริง (31 ก.พ.) · `""` → `null` |

**เทสรันบนเครื่องที่ตั้งเป็น UTC โดยตั้งใจ** (ดู [vitest.config.mts](vitest.config.mts)) เพราะสิ่งที่ต้องพิสูจน์คือ "วันนี้" ไม่ได้มาจากเวลาเครื่อง ถ้ารันบนเครื่องที่ตั้งเวลาไทยอยู่แล้ว เทสจะผ่านทั้งที่โค้ดพัง แล้วไปพังจริงบน Vercel ซึ่งรันเป็น UTC

## ประสิทธิภาพ

วัดกับ Supabase จริง (ap-south-1 จากไทย)

```
select 1 (round trip ล้วน)   90 ms
ยอดบัญชี + ยอดรวม            90 ms
สรุปรายเดือน + แยกรายวัน      90 ms
```

query ที่ซับซ้อนที่สุดใช้เวลาเท่ากับ `select 1` เป๊ะ แปลว่า**เวลาทั้งหมดคือระยะทางของเน็ต ไม่ใช่การประมวลผล** ดัชนีและ SQL ไม่มีอะไรต้องปรับ

ทุกหน้ายิง query แบบขนานด้วย `Promise.all` เสมอ หนึ่งหน้าจึงเสียเวลาเท่ากับ round trip เดียว ไม่ใช่บวกกันทีละ query

> **ถ้าอยากให้เร็วขึ้นอีกเท่าตัว** ย้าย Supabase ไป region `ap-southeast-1` (สิงคโปร์) ซึ่งใกล้ไทยกว่า Mumbai มาก — Supabase เปลี่ยน region ของโปรเจกต์เดิมไม่ได้ ต้องสร้างโปรเจกต์ใหม่แล้วรัน `supabase/schema.sql` ทำตอนนี้ที่ยังไม่มีข้อมูลจริงจะถูกที่สุด

## โครงสร้าง

```
src/
├── app/
│   ├── login/          ใส่ PIN
│   ├── shops/          เลือกร้าน — ต้องผ่านหน้านี้ก่อนเข้าแอป
│   ├── (app)/          หน้าที่ต้องล็อกอินและเลือกร้านแล้ว
│   ├── api/export/     ทางออกของข้อมูล JSON + CSV
│   └── manifest.ts     ปักหน้าจอโฮมได้
├── actions/            server action ทั้งหมด — ทุกตัวตรวจสิทธิ์เอง
├── components/         ชิ้นส่วน UI ที่ใช้ซ้ำ
├── db/
│   ├── schema.ts       นิยามตาราง — แหล่งความจริงเดียวของโครงสร้างข้อมูล
│   ├── queries.ts      การอ่านทั้งหมด — การรวมยอดเงินอยู่ที่นี่ที่เดียว
│   └── seed.ts
└── lib/
    ├── date.ts         วันที่ทั้งหมดตรึงที่ Asia/Bangkok
    ├── money.ts        กฎการจัดการเงิน
    ├── auth.ts         PIN + session
    └── validation.ts   Zod schema ทุกตัว
```

การ deploy และการย้ายโฮสต์ → **[docs/DEPLOY.md](docs/DEPLOY.md)**

---

## ขอบเขตของ v1

หน้าตั้งค่ามีแค่ **บัญชี · ประเภท · ส่งออกข้อมูล** ตั้งใจไม่ใส่มากกว่านี้ เพราะหน้าตั้งค่าที่ยาวเป็นหางว่าวทำให้หาของที่ต้องใช้จริงไม่เจอ โดยเฉพาะบนมือถือ

**ยังไม่ได้ทำ**

- ตารางสินค้าและกำไรรายชิ้น
- ขายเชื่อ ซื้อเชื่อ
- ระบบผู้ใช้แยกคน — ตอนนี้ PIN เดียวใช้ร่วมกัน จึงไม่รู้ว่าใครบันทึกอะไร ถ้าต้องสืบย้อนได้ต้องเพิ่มตาราง users กับคอลัมน์ `created_by`
- บัญชีที่ใช้ร่วมเฉพาะบางร้าน — ตอนนี้เป็นได้แค่ร่วมทุกร้านหรือร้านเดียว ถ้าต้องการต้องเพิ่มตาราง `shop_accounts`
- กู้ของที่ลบไปแล้วจากหน้าเว็บ — ข้อมูลยังอยู่ครบ แต่ต้องกู้ด้วย SQL
- ไอคอน `public/icon-192.png` และ `icon-512.png` ต้องใส่เอง
